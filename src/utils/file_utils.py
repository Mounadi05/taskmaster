# File and directory utilities
# File operations, path handling, and directory management
# parser = argparse.ArgumentParser(description='Taskmaster Job Control Daemon')
# parser.add_argument('-c', '--config', 
#                     default='config/taskmaster.yaml',
#                     help='Configuration file path (default: config/taskmaster.yaml)')
# parser.add_argument('-l', '--log-level',
#                     choices=['DEBUG', 'INFO', 'WARNING', 'ERROR'],
#                     default='INFO',
#                     help='Log level (default: INFO)')
# parser.add_argument('--log-file',
#                     default='logs/taskmaster.log',
#                     help='Log file path (default: logs/taskmaster.log)')
# parser.add_argument('-d', '--daemon',
#                     action='store_true',
#                     help='Run as daemon (background process)')